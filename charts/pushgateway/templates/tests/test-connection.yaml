apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pushgateway.fullname" . }}-test-connection"
  labels:
    {{- include "pushgateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pushgateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
