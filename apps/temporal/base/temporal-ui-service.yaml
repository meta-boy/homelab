apiVersion: v1
kind: Service
metadata:
  name: temporal-ui
  namespace: temporal
spec:
  type: NodePort
  selector:
    app: temporal-ui
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30088
    protocol: TCP
