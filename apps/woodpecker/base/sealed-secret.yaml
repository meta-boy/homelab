apiVersion: v1
kind: Secret
metadata:
  name: woodpecker-secret
  namespace: woodpecker
type: Opaque
stringData:
  # Generate a random agent secret with: openssl rand -hex 32
  agent-secret: "REPLACE_WITH_AGENT_SECRET"
  # Optional: Set admin username
  admin-user: ""
