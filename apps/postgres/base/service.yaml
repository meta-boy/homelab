apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: postgres
spec:
  type: NodePort
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
    nodePort: 30432  # Exposed on node IP at port 30432 (NodePort range: 30000-32767)
    protocol: TCP
    name: postgres
