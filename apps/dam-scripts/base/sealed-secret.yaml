apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: dam-scripts-secret
  namespace: dam-scripts
spec:
  encryptedData:
    # TODO: Seal these values using kubeseal
    # Values to encrypt:
    # USERNAME: midc_savitri@123
    # PASSWORD: savitri@123
    # SITE_ID: "95"
    # PUSHGATEWAY_URL: http://pushgateway.monitoring.svc.cluster.local:9091
    # PUSHGATEWAY_JOB_NIMBUS: nimbus_water_level
    # PUSHGATEWAY_JOB_METRO: midc_metro_scraper
    USERNAME: PLACEHOLDER_TO_BE_SEALED
    PASSWORD: PLACEHOLDER_TO_BE_SEALED
    SITE_ID: PLACEHOLDER_TO_BE_SEALED
    PUSHGATEWAY_URL: PLACEHOLDER_TO_BE_SEALED
    PUSHGATEWAY_JOB_NIMBUS: PLACEHOLDER_TO_BE_SEALED
    PUSHGATEWAY_JOB_METRO: PLACEHOLDER_TO_BE_SEALED
  template:
    metadata:
      name: dam-scripts-secret
      namespace: dam-scripts
    type: Opaque
