apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dam-scripts
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/meta-boy/homelab.git
    targetRevision: main
    path: apps/dam-scripts/base
  destination:
    server: https://kubernetes.default.svc
    namespace: dam-scripts
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
